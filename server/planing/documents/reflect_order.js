
module.exports = function({client, utils, orders}) {
  if(orders) {
    orders.set(calc_order, 0);
  }
  if (doc.class_name = 'doc.work_centers_task') {
    for(const {calc_order} of doc.set) {
      orders.set(calc_order, 0);
    }
  }
}
